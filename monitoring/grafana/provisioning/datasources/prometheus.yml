# Provisionamento automatico de datasources do Grafana para o projeto Muglia

apiVersion: 1

datasources:
  # Prometheus como fonte principal de metricas
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false  # Gerenciado por codigo, nao editar manualmente
